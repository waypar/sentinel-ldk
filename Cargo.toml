[package]
name = "sentinel-ldk"
version = "0.1.0"
edition = "2024"
license = "MIT"

[build-dependencies]
bindgen = "0.72.1"
syn = { version = "2", features = ["full"] }

[dev-dependencies]
# Required to test build_hasp_errors.rs
syn = { version = "2", features = ["full"] }
